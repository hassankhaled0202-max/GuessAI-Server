import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hassan.guessai',
  appName: 'GuessAI',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;